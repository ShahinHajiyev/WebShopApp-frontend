import { MyEmptySpaceValidator } from './my-empty-space-validator';

describe('MyEmptySpaceValidator', () => {
  it('should create an instance', () => {
    expect(new MyEmptySpaceValidator()).toBeTruthy();
  });
});
